# QeMix数字人平台 部署安装文档

本文说明如何装环境、放置模型、启动网页服务。

网页质量档：标准、高质量、超高质量。单卡同时只跑一条合成。

## 1. 硬件与系统

- GPU：NVIDIA 显卡，显存建议 24GB
- 系统：Linux + NVIDIA 驱动 + CUDA
- 单卡同时只跑一条合成

## 2. 安装环境

在工程根目录执行官方环境脚本（会创建独立 Python 3.10 环境并安装依赖）：

```bash
source setup_env.sh
```

环境若已经存在，激活后即可。API 额外需要：

```bash
pip install fastapi uvicorn python-multipart
```

## 3. 模型文件

权重统一放在工程下的 `checkpoints/`，网页所有任务共用。需要：

- 主合成模型
- 音频特征模型
- 图像编解码模型
- 人脸检测模型（首次合成时会自动补齐）

国内下载慢时可先设镜像：

```bash
export HF_ENDPOINT=https://hf-mirror.com
```

训练用的对照网络推理不需要。

## 4. 启动服务

在工程根目录：

```bash
bash webapp/start.sh
```

浏览器打开：`http://<机器IP>:8765`

首次进入需输入访问密钥，默认是 `King`。

在线接口文档：`http://<机器IP>:8765/docs`

文字版见工程根目录 `数字人API接口文档.md`。

- 网页数据：`webapp/storage/`（上传、形象、任务、成片）
- 模型权重：`checkpoints/`

## 5. 素材建议

建议视频先转成 25fps、8bit H.264，避免 10bit HEVC 占用过多内存。
