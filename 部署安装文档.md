# QeMix数字人平台 部署安装文档

本文说明如何装环境、放置模型、启动网页服务。

网页质量档：标准、高质量、超高质量。单卡同时只跑一条合成。

## 1. 硬件与系统

- GPU：NVIDIA 显卡，显存建议 24GB
- 系统：Linux + NVIDIA 驱动 + CUDA
- 单卡同时只跑一条合成

## 2. 安装环境

在工程根目录激活已有环境即可。原工程安装脚本在 `upstream/setup_env.sh`。API 额外需要：

```bash
pip install fastapi uvicorn python-multipart
```

## 3. 模型文件

权重统一放在工程下的 `checkpoints/`，网页所有任务共用。需要：

- 主合成模型
- 音频特征模型
- 图像编解码模型
- 人脸检测模型（首次合成时会自动补齐）

国内下载慢时可先设镜像：

```bash
export HF_ENDPOINT=https://hf-mirror.com
```

训练用的对照网络推理不需要。

## 4. 启动服务

监听地址、端口、访问密钥、GPU 都写在根目录 `config.yaml`。网页接口和 GPU 合成是两个进程：接口只收请求、写队列；真正跑模型的是 worker。重启或关掉网页服务，不会中断正在合成的任务。

在工程根目录（用你的 Python 环境）：

```bash
python start.py
```

浏览器打开：`http://<机器IP>:8811`（端口以 `config.yaml` 为准）

首次进入需输入访问密钥，默认是 `King`，同样写在 `config.yaml`。

只停合成 worker（进行中的 GPU 任务仍会跑完，重启 `start.py` 后会重新接管）：

```bash
kill "$(cat webapp/storage/worker.pid)"
```

`config.yaml` 示例：

```yaml
host: 0.0.0.0
port: 8811
admin_key: King
gpus: [0]        # 单卡
# gpus: [0, 1]   # 多卡并行，每张卡同时只跑一条
```

GPU 编号与 `nvidia-smi` 一致。改 GPU 一般马上生效，正在跑的任务不受影响。改 host/port 后需要重新执行 `python start.py`。

在线接口文档：`http://<机器IP>:8811/docs`

若前面有反向代理（例如 `http://公网IP:8888/任意前缀/`），把该前缀转到本机端口即可。网页、API、`/docs` 会按当前访问地址自动带上前缀，项目里不用写死路径。

文字版见工程根目录 `QeMixAvatar_API接口文档.md`。

- 网页数据：`webapp/storage/`（上传、形象、任务、成片）
- 合成 worker 日志：`webapp/storage/worker.log`
- 模型权重：`checkpoints/`
- 原工程训练 / 评测 / 演示脚本：`upstream/`

## 5. 素材建议

建议视频先转成 25fps、8bit H.264，避免 10bit HEVC 占用过多内存。
